#include<stdio.h>
main(){
    // long nc;
    double nc;
    // nc=0;
    // while(getchar()!=EOF)
    //     ++nc;
    // printf("%ld\n",nc);
    for(nc=0;getchar()!=EOF;)
            printf("%.0f\n",++nc);;

}